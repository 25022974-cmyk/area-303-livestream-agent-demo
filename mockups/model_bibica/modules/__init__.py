"""Package modules AREA_303 — Loader + 5 module quyết định + Learner + Pipeline.

Tất cả module stdlib thuần (không import fastapi/numpy/sklearn) để dùng được cả
trong `inspect` mode (chạy trực tiếp) lẫn server FastAPI.
"""
